pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Research to Reinvent Yourself"
                sh 'echo using shell within Jenkinsfile'
                echo 'not using shell in the Jenkinsfile'
            }
        }
        stage('test') {
            steps {
                echo "testing"
            }
        }
    }
}